{
  "name": "ai-pm",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"pnpm --filter @ai-pm/api dev\" \"pnpm --filter @ai-pm/web dev\"",
    "seed": "pnpm --filter @ai-pm/database run seed",
    "build": "pnpm --filter @ai-pm/shared build && pnpm --filter @ai-pm/database build && pnpm --filter @ai-pm/domain build && pnpm --filter @ai-pm/project-state build && pnpm --filter @ai-pm/ai build && pnpm --filter @ai-pm/api build && pnpm --filter @ai-pm/web build",
    "typecheck": "pnpm -r --workspace-concurrency=1 run typecheck",
    "test": "pnpm -r --workspace-concurrency=1 run test"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "typescript": "^5.6.3"
  }
}
